## Use UUID v7 as Entity Identifiers

Context

The domain requires entity identifiers that are independent from the persistence layer.

Decision

All aggregate roots and entities will use UUID v7 generated by the domain instead of auto-increment integer identifiers.

Consequences

Domain entities are persistence independent.
Better index locality than UUID v4.
Global uniqueness.
Easier future migration to distributed architectures.
